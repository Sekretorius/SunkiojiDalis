import { AbstractArmor } from '../AbstractArmor';

export class LegendaryArmor extends AbstractArmor {
  constructor(guid: string, itemData: any, imageSharedData) {
    super(guid, itemData, imageSharedData);
  }
}
